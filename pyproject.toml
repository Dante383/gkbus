[build-system]
requires = ["setuptools"]

[project]
name = "gkbus"
authors = [{name = "Dante", email = "dante383@protonmail.com"}]
version = "0.4.1"
dynamic = ["description","dependencies","readme"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}
readme = {file=["README.md"], content-type="text/markdown"}
